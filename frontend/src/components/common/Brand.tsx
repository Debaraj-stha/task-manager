import React from 'react'
import { APP_NAME } from '../../constants/content'

const Brand = () =>  <h2 className='font-semibold text-2xl md:text-3xl  whitespace-nowrap'>{APP_NAME}</h2>

export default Brand